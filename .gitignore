.build/
.swiftpm/
*.xcodeproj
DerivedData/
dist/
*.app
.DS_Store

# Agent worktrees. Kept inside the repo so they're easy to find, ignored so
# they never show up as untracked noise in the main checkout.
.worktrees/
